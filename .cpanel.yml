---
deployment:
  tasks:
    - export DEPLOYPATH=/home/andofoodvn/repositories/andong-food-website
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH/backend && npm install --production
    - mkdir -p $DEPLOYPATH/backend/tmp
    - touch $DEPLOYPATH/backend/tmp/restart.txt
